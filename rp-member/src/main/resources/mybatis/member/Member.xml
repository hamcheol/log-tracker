<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rp.member.repository.MemberRepositoryImpl">
	<select id="selectMembers" parameterType="com.rp.member.model.Member" resultType="com.rp.member.model.Member">
		SELECT 
			id as id,
			name as name,
			addr as addr,
			sex as sex,
			birth as birth
		FROM rp_member
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="id != null">
			AND id = #{id}
			</if>
			<if test="name != null">
			AND name = #{name}
			</if>
			<if test="sex != null">
			AND sex = #{sex}
			</if>
		</trim>
	</select>
	
	<insert id="insertMember" parameterType="com.rp.member.model.Member">
		INSERT INTO rp_member (
			id,
			name,
			addr,
			sex,
			birth
		)values(
			#{id},
			#{name},
			#{addr},
			#{sex},
			#{birth}
		)
	</insert>
</mapper>