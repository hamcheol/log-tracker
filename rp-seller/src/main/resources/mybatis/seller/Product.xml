<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rp.seller.repository.ProductRepositoryImpl">
	<insert id="insertProduct" parameterType="com.rp.seller.model.Product">
		INSERT INTO rp_product (
			id,
			name,
			price,
			seller_id,
			info
		) values (
			#{id},
			#{name},
			#{price},
			#{seller.id},
			#{info}
		)
	</insert>
	
	<select id="selectProducts" parameterType="com.rp.seller.model.Product" resultType="com.rp.seller.model.Product">
		SELECT
			id as id,
			name as name,
			price as price,
			seller_id as sellerId,
			info as info
		FROM rp_product
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="id != null">
			AND id = #{id}
			</if>
			<if test="name != null">
			AND name = #{name}
			</if>
			<if test="sellerId != null">
			AND seller_id = #{sellerId}
			</if>
		</trim>
	</select>
</mapper>